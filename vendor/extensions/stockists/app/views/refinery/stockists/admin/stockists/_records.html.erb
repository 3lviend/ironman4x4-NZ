<%= render 'refinery/admin/search_header', :url => refinery.stockists_admin_stockists_path %>
<div class='pagination_container'>
  <% if @stockists.any? %>
    <%= render 'stockists' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
