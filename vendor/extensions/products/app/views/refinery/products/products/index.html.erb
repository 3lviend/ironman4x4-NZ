<% content_for :body do %>
  <ul id="products">
    <% @products.each do |product| %>
      <li>
        <%= link_to product.product_no, refinery.products_product_path(product) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
