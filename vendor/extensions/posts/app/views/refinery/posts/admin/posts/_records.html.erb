<%= render 'refinery/admin/search_header', :url => refinery.posts_admin_posts_path %>
<div class='pagination_container'>
  <% if @posts.any? %>
    <%= render 'posts' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
