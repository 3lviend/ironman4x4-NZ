<%= render 'refinery/admin/search_header', :url => refinery.ironman_admin_vehicles_path %>
<div class='pagination_container'>
  <% if @vehicles.any? %>
    <%= render 'vehicles' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
