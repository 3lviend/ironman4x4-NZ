<%= render 'refinery/admin/search_header', :url => refinery.ironman_admin_warehouses_path %>
<div class='pagination_container'>
  <% if @warehouses.any? %>
    <%= render 'warehouses' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
