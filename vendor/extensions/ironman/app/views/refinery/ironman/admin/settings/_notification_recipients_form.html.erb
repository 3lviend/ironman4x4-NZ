<%= form_for @setting, :url => refinery.ironman_admin_setting_path(@setting) do |f| %>

  <div class='field'>
    <span class='label_with_help'>
      <%= f.label :value, t('.enquiry_settings_value_name') %>
    </span>
    <%= f.text_field :value, :value => f.object.value, # required, we can't use :value
                     :class => "larger widest" %>
  </div>

  <p>
    <%= t('.hint') %>
  </p>
  <p>
    <%= t('.example') %>
  </p>

  <%= render :partial => "/refinery/admin/form_actions",
             :locals => {
               :f => f,
               :continue_editing => false,
               :cancel_url => refinery.ironman_admin_enquiries_path,
               :hide_delete => true
             } %>
<% end %>
